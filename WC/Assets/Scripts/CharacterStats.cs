using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterStats : MonoBehaviour
{

    //Spawn this in the character position spot
    [SerializeField] public Sprite characterSelectionScreenSprite;
    [SerializeField] public GameObject characterSelectionScreenImagePrefab;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

}
